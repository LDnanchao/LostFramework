using I2.Loc;
using TMPro;

namespace QFramework
{
    public static class I10nKitHelp
    {
        //public static void SetParameterValue(this TextMeshProUGUI text, string key, object value)
        //{
        //    if (text == null)
        //        return;
        //    LocalizationParamsManager localizationParamsManager = text.GetComponent<LocalizationParamsManager>();
        //    if (localizationParamsManager == null)
        //    {
        //        localizationParamsManager = text.gameObject.AddComponent<LocalizationParamsManager>();
        //    }
        //    localizationParamsManager.SetParameterValue(key, value.ToString());
        //}

        //public static void SetTerm(this TextMeshProUGUI text,string term)
        //{
        //    if (text == null)
        //        return;

        //    Localize localize = text.GetComponent<Localize>();
        //    if(localize == null)
        //    {
        //        localize = text.gameObject.AddComponent<Localize>();
        //    }
        //    localize.SetTerm(term);
        //}
    }
}

